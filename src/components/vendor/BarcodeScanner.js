import React from 'react';
import { Box, Typography } from '@mui/material';

const BarcodeScanner = () => {
  return (
    <Box>
      <Typography>Barcode Scanner Component</Typography>
    </Box>
  );
};

export default BarcodeScanner; 